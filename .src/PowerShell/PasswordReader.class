' Gambas class file

'''  Prompts for a password to pass to sudo.\
''' Main application framework looks for the name to start with Password_\
''' The powershell class creates links to the original App with this  name and then
''' invokes the sudo command with the link name.\
''' When it does then this form is opened and executed printing the
''' password entered to the standard out as required by SUDO
Export

Public Sub btnOK_Click()

  Print PasswordValue.text
  PasswordReader.Close()

End

Public Sub PasswordValue_KeyPress()

  'testbox.text = Str$(Asc(Mid(Key.Text, 1)))
  If Asc(Mid(Key.text, 1)) = 13 Then
    btnOK_Click()
  Endif

End
